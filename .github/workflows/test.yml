name: Test

on:
  pull_request:
  workflow_call:
  workflow_dispatch:

jobs:
  test:
    name: Theory tests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 'lts/*'
      - name: Run tests
        run: npm test
